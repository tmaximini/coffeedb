# import * from './generated/prisma.graphql'

type Mutation {
  createCoffee(title: String, description: String): Coffee!
}

type Query {
  coffees: [Coffee]!
  coffee(where: CoffeeWhereInput): Coffee
  users: [User]!
}
